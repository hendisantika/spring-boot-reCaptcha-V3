services:
  mysql:
    image: mysql:9.4.0
    environment:
      MYSQL_ROOT_PASSWORD: 53cret
      MYSQL_DATABASE: recaptcha_db
      MYSQL_USER: yu71
      MYSQL_PASSWORD: 53cret
    ports:
      - "3308:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  mysql_data: